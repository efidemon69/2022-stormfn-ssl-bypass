#pragma once

#define BACKEND_HOST "backend.nigger.shop" // replace with your backend's domain name, example backend.stormfn.wtf, and if you have a VPS then put the IP of it without protocol
#define BACKEND_PORT "1337" // replace with your backend port

// recaptcha (not needed at all since were not communicating with the backend)
#define API_KEY 0
#define API_SECRET_KEY 0